import { Page} from "@playwright/test";

export default class UsersPage{

    constructor(public page: Page){}

    //variables

    //search name, email (fillable)
    //status drop down
        /**
         * active
         * inactive
         */
    //clear button
    //row counter
        /**
         * 15
         * 30
         * 50
         */
    //add users button
        /**fillable fields
         * first name
         * middle name
         * last name
         * title
         * suffix
         * credentials
         * email
         */
    //client admin (checkbox)
    //contents (checkbox)
    //users (checkbox)
    //clinical navigation (checkbox)
    //reports (checkbox)
    //facility names (checkboxes)
    //status drop down
        /**
         * active
         * inactive
         */
    //save user button
    //back arrow button
    

}